"""Suite de testes do CatalogFlow.

- tests/conftest.py — fixtures globais (DB, client, storage mock).
- tests/integration/ — pipelines completos (DB real, S3 mockado).
- tests/e2e/ — flows HTTP completos via httpx.
- tests/fixtures/ — gerador de PDFs sintéticos para testes.
"""
