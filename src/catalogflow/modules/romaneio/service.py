"""RomaneioService — geração e download do romaneio.

Esqueleto reservado para Sprint 02. Operações previstas:

    - generate_romaneio(order_id) → (Romaneio, Job)
    - get_download_url(romaneio_id, brand_id) → presigned URL
"""

from __future__ import annotations


class RomaneioService:
    """Serviço do domínio `romaneio`. Implementação na Sprint 02."""

    def __init__(self, *args: object, **kwargs: object) -> None:
        _ = args, kwargs
        raise NotImplementedError("RomaneioService entra na Sprint 02")
